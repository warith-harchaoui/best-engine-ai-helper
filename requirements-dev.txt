pytest>=8.0
pytest-cov>=5.0
ruff==0.15.21
mypy==1.20.2
pre-commit>=3.7
types-pyyaml
types-requests
httpx>=0.27
pillow>=10
# Message types (HumanMessage/SystemMessage) for the langchain backend path in
# llm.py. Test-only: the backend wrappers (langchain_ollama / langchain_openai)
# are stubbed in tests/test_llm.py, but the message import is unconditional, so
# langchain-core must be importable for those tests to run.
langchain-core>=0.3
